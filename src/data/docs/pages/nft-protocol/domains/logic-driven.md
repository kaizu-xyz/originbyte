# Logic-driven domains

Logic-driven domains add behaviour, not just data — access policies, mutability locks, and other rules that fire when an NFT is transferred, borrowed, or mutated. See [Permissions](/docs/nft-protocol/permissions) and [Royalty enforcement](/docs/nft-protocol/royalty).
